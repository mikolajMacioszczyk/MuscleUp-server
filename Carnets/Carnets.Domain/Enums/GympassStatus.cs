namespace Carnets.Domain.Enums
{
    // TODO: Create state machine
    public enum GympassStatus
    {
        None = 0,
        New = 1,
        Cancelled = 2,
        Paid = 3,
        Active = 4,
        Inactive = 5,
        Completed = 6,
    }
}
